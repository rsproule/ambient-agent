# ============================================
# Required Environment Variables
# ============================================

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/dbname?schema=public"

# Trigger.dev
TRIGGER_SECRET_KEY="your-trigger-secret-key"
TRIGGER_PROJECT_ID="imessage-pipeline"  # Optional, defaults to this
# TRIGGER_API_URL="https://api.trigger.dev"  # Optional, for custom API URL

# LoopMessage (iMessage API)
# Note: For local development, use LoopMessage sandbox mode
# This allows webhooks to be delivered locally without needing a full phone number
LOOP_AUTH_KEY="your-loop-auth-key"
LOOP_SECRET_KEY="your-loop-secret-key"
LOOP_SENDER_NAME="your-sender-name"
LOOP_WEBHOOK_SECRET_KEY="your-webhook-secret-key"

# NextAuth (Magic Link Authentication)
NEXTAUTH_SECRET="generate-a-random-secret-here"  # Use: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"  # Optional, defaults based on request
NEXT_PUBLIC_BASE_URL="http://localhost:3000"  # Optional, for public URLs

# ============================================
# Optional: AI Provider API Keys
# ============================================

# Echo Router (Merit Systems) - Used for Anthropic Claude models
# If not set, will fall back to direct ANTHROPIC_API_KEY
ECHO_API_KEY="your-echo-api-key"

# Direct AI Provider Keys (if not using Echo router)
# ANTHROPIC_API_KEY="your-anthropic-key"
# OPENAI_API_KEY="your-openai-key"  # Required for embeddings
# PERPLEXITY_API_KEY="your-perplexity-key"  # For web search
# XAI_API_KEY="your-xai-key"  # For X/Grok search
# GOOGLE_GENERATIVE_AI_API_KEY="your-google-key"  # For Gemini image generation

# ============================================
# Optional: Pipedream (OAuth Connections)
# ============================================
# Required if you want to use Gmail, Calendar, or GitHub integrations
# PIPEDREAM_CLIENT_ID="your-pipedream-client-id"
# PIPEDREAM_CLIENT_SECRET="your-pipedream-client-secret"
# PIPEDREAM_PROJECT_ID="your-pipedream-project-id"
# PIPEDREAM_PROJECT_ENVIRONMENT="development"  # or "production"
# PIPEDREAM_OAUTH_APP_ID="your-oauth-app-id"

# ============================================
# Optional: Monitoring & Logging
# ============================================
# CRON_SECRET="your-cron-secret"  # For protecting cron endpoints
# LOG_FORMAT="pretty"  # or "json"
# LOG_LEVEL="info"  # debug, info, warn, error
# LOG_FILE="logs/app.log"  # Optional file logging

# ============================================
# Optional: Telemetry (Signoz)
# ============================================
# SIGNOZ_OTLP_ENDPOINT="https://your-signoz-endpoint"
# SIGNOZ_INGESTION_KEY="your-signoz-key"

# ============================================
# Node Environment
# ============================================
NODE_ENV="development"
